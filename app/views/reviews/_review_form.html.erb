
<%= form_for [@brewery, @beer, @review] do |f| %>

<%= f.hidden_field :user_id, :value => current_user.id %>

<textarea name="review[body]" cols="80" rows="7"><%= @review.body %></textarea>
<br>
<%= f.submit %>

<% end %>
